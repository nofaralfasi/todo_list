<nav class='navbar navbar-expand-lg navbar-light' style='background-color: #e3f2fd;'>
  <div class='collapse navbar-collapse' id='navbarNavAltMarkup'>
    <div class='navbar-nav mr-auto mt-2 mt-lg-0'>
      <%= link_to 'Home', lists_path, class: 'nav-item nav-link active' %>
      <%= link_to 'Labels', labels_path, class: 'nav-item nav-link' %>
      <% if @list&.persisted? && @task %>
        <%= link_to 'Back to list', list_path(@list), class: 'nav-item nav-link' %>
      <% end %>
    </div>
  </div>
</nav>
